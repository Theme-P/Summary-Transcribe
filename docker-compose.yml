services:
  whisperx:
    build: .
    container_name: whisperx
    stdin_open: true
    tty: true
    runtime: nvidia
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    volumes:
      - ./audio:/app/audio
    env_file:
      - .env
